% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blm.R
\name{blm}
\alias{blm}
\title{Bayesian linear regression}
\usage{
blm(
  y,
  ETA,
  residual_var = NULL,
  residual_shape = NULL,
  residual_scale = NULL,
  iterations = 4000L,
  burnin = 1000L,
  thin = 1L,
  seed = NULL,
  version = c("Rcpp", "R"),
  verbose = FALSE,
  nchains = 1L,
  store_samples = TRUE,
  store_coefficient_cov = TRUE
)
}
\arguments{
\item{y}{A finite numeric response vector.}

\item{ETA}{A predictor specification or named list of predictor
specifications. Each block must contain \code{X} and \code{model}. Available models
are \code{"Normal"}, \code{"SpikeSlab"}, \code{"GlobalLocal"}, and
\code{"SpikeMultiSlab"}. See Details.}

\item{residual_var}{A positive known residual variance, or \code{NULL} to learn
it using an inverse-gamma prior.}

\item{residual_shape, residual_scale}{Positive shape and scale parameters for
the inverse-gamma residual-variance prior. Required when
\code{residual_var = NULL}.}

\item{iterations}{Total Gibbs iterations when sampling is required.}

\item{burnin}{Number of initial iterations to discard.}

\item{thin}{Interval between retained draws.}

\item{seed}{\code{NULL} or an integer random-number seed.}

\item{version}{Gibbs implementation: \code{"Rcpp"} or \code{"R"}.}

\item{verbose}{If \code{TRUE}, display aggregate progress at 10-percent intervals
per chain using \code{\link[progressr:with_progress]{progressr::with_progress()}}.}

\item{nchains}{Number of independent chains. Multiple chains use a temporary
\link[future:multisession]{future::multisession} plan.}

\item{store_samples}{If \code{TRUE} (the default), retain and return individual
posterior draws. If \code{FALSE}, compute posterior summaries online without
allocating draw matrices. Fits without stored samples cannot be passed to
\code{\link[=assess_convergence]{assess_convergence()}}.}

\item{store_coefficient_cov}{If \code{TRUE} (the default), return the full
posterior coefficient covariance matrix for each \code{ETA} block. If \code{FALSE},
return only the named vector of marginal coefficient variances and avoid
the quadratic-size covariance accumulator when samples are not stored.}
}
\value{
An object of class \code{blm_fit}: a list containing \code{ETA}, a named list
of block-specific posterior
summaries, plus intercept and residual-variance summaries. Every block
contains a named \code{coefficient_var} vector; \code{coefficient_cov} is included
when \code{store_coefficient_cov = TRUE}. When
\code{store_samples = TRUE}, the corresponding posterior draws are also
returned. With multiple chains, \code{chain_id} identifies the origin of each
retained draw when samples are stored. \code{SpikeMultiSlab} blocks additionally
return per-predictor \code{component_probability} values, an
\code{inclusion_probability} vector, and summaries of the mixture probabilities
and shared variance.
}
\description{
Fits a Bayesian linear regression with one or more predictor blocks using a
BGLR-style \code{ETA} interface. Each block specifies its predictors,
coefficient-prior family, standardization, and prior parameters. The
intercept is integrated out during coefficient sampling by centering the
response and predictors.
}
\details{
\code{ETA} may be a single-block specification such as
\code{list(X = X, model = "Normal")}, or a named list of blocks.
A numeric vector supplied as a block's \code{X} is treated as a one-column
matrix.
Every block accepts \code{standardize}, which defaults to \code{TRUE}. Returned
coefficients are always transformed to the original scale of that block's
supplied \code{X}.

A \code{"Normal"} block optionally accepts \code{var_shape = 2} and
\code{var_scale = 1}. Its coefficients share a variance sampled from an
inverse-gamma prior with the supplied shape and scale.
A \code{"SpikeSlab"} block optionally accepts \code{pi = c(a = 1, b = 1)},
\code{var_shape = 2}, and \code{var_scale = 1}. Its shared slab variance has an
inverse-gamma prior with the supplied shape and scale. A \code{"GlobalLocal"}
block optionally accepts
\code{local_shape = c(a = 1, b = 0.5)} and \code{global_scale = 1}. Its hierarchy is
\deqn{\beta_j \mid \tau^2,\psi_j \sim N(0,\tau^2\psi_j),\qquad
  \psi_j \sim \mathrm{BetaPrime}(a,b),}
with \eqn{\tau \sim C^+(0,\mathrm{global\_scale})}. Thus the default is
Strawderman-Berger, while \code{local_shape = c(0.5, 0.5)} gives the horseshoe.

A \code{"SpikeMultiSlab"} block uses a BayesR-style mixture containing a point
mass at zero followed by normal slabs. It optionally accepts
\code{gamma = c(0, 0.01, 0.1, 1)}, a strictly increasing vector whose first
element is zero; \code{alpha = rep(1, length(gamma))}, the Dirichlet prior
concentrations for the component probabilities; and \code{var_shape = 2} and
\code{var_scale = 1} for the inverse-gamma prior on the shared variance. Given
component \eqn{c > 1}, the coefficient variance is
\eqn{\gamma_c \sigma_\beta^2}.
The coefficient priors are independent of the residual variance.
}
\examples{
X <- cbind(x1 = 1:20, x2 = rep(c(0, 1), 10))
y <- 1 + 2 * X[, "x1"] - X[, "x2"]
blm(
  y,
  ETA = list(X = X, model = "Normal", var_shape = 2, var_scale = 10),
  residual_var = 1
)
blm(
  y,
  ETA = list(markers = list(X = X, model = "GlobalLocal")),
  residual_shape = 2,
  residual_scale = 1,
  iterations = 100,
  burnin = 50,
  seed = 123
)
}
