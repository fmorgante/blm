% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blm_ss.R
\name{blm_ss}
\alias{blm_ss}
\title{Bayesian linear regression from sufficient statistics}
\usage{
blm_ss(
  n,
  XtX,
  Xty,
  ETA,
  yty = NULL,
  X_means = NULL,
  y_mean = NULL,
  residual_var = NULL,
  residual_shape = NULL,
  residual_scale = NULL,
  iterations = 4000L,
  burnin = 1000L,
  thin = 1L,
  seed = NULL,
  version = c("Rcpp", "R"),
  verbose = FALSE,
  nchains = 1L,
  store_samples = TRUE,
  store_coefficient_cov = TRUE
)
}
\arguments{
\item{n}{Number of observations used to form the sufficient statistics.}

\item{XtX}{A finite, symmetric predictor cross-product matrix.}

\item{Xty}{A finite predictor-response cross-product vector.}

\item{ETA}{A prior specification or named list of prior blocks. Each block
must contain \code{model} and may contain \code{indices}, an integer or character
vector selecting columns of \code{XtX}. A single block may omit \code{indices} and
then uses every predictor. Multiple blocks must partition all predictors.
Prior parameters and \code{standardize} are the same as in \code{\link[=blm]{blm()}}.}

\item{yty}{Optional finite, nonnegative response sum of squares. It is
required when \code{residual_var = NULL}.}

\item{X_means, y_mean}{Optional predictor means and response mean. They must
be supplied together. When omitted, the model is fitted without an
intercept.}

\item{residual_var}{A positive known residual variance, or \code{NULL} to learn
it using an inverse-gamma prior.}

\item{residual_shape, residual_scale}{Positive shape and scale parameters for
the inverse-gamma residual-variance prior. Required when
\code{residual_var = NULL}.}

\item{iterations}{Total Gibbs iterations when sampling is required.}

\item{burnin}{Number of initial iterations to discard.}

\item{thin}{Interval between retained draws.}

\item{seed}{\code{NULL} or an integer random-number seed.}

\item{version}{Gibbs implementation: \code{"Rcpp"} or \code{"R"}.}

\item{verbose}{If \code{TRUE}, display aggregate progress at 10-percent intervals
per chain using \code{\link[progressr:with_progress]{progressr::with_progress()}}.}

\item{nchains}{Number of independent chains. Multiple chains use a temporary
\link[future:multisession]{future::multisession} plan.}

\item{store_samples}{If \code{TRUE} (the default), retain and return individual
posterior draws. If \code{FALSE}, compute posterior summaries online without
allocating draw matrices. Fits without stored samples cannot be passed to
\code{\link[=assess_convergence]{assess_convergence()}}.}

\item{store_coefficient_cov}{If \code{TRUE} (the default), return the full
posterior coefficient covariance matrix for each \code{ETA} block. If \code{FALSE},
return only the named vector of marginal coefficient variances and avoid
the quadratic-size covariance accumulator when samples are not stored.}
}
\value{
A fitted object with the same block-specific posterior summaries as
\code{\link[=blm]{blm()}}. Intercept components are present only when both \code{X_means} and
\code{y_mean} are supplied.
}
\description{
Fits the same prior models as \code{\link[=blm]{blm()}} using cross-products instead of the
original response and predictor matrix.
}
\details{
If means are supplied, \code{XtX}, \code{Xty}, and \code{yty} are interpreted as
uncentered cross-products and are centered internally. Without means,
they are used as supplied. If any block requests standardization without
means, a warning reminds the user that the supplied cross-products should
already represent centered or standardized variables.
}
\examples{
X <- cbind(x1 = 1:20, x2 = rep(c(0, 1), 10))
y <- 1 + 2 * X[, "x1"] - X[, "x2"]
fit <- blm_ss(
  n = nrow(X),
  XtX = crossprod(X),
  Xty = drop(crossprod(X, y)),
  ETA = list(model = "Normal"),
  yty = sum(y^2),
  X_means = colMeans(X),
  y_mean = mean(y),
  residual_var = 1,
  iterations = 100,
  burnin = 50,
  seed = 123
)
}
